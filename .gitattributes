# Normalise line endings to LF in the repo; checkout as CRLF on Windows
* text=auto eol=crlf

# Keep these as LF regardless (shell scripts, GitHub Actions)
*.yml  text eol=lf
*.yaml text eol=lf
*.sh   text eol=lf

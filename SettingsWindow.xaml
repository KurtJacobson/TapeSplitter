<Window x:Class="TapeSplitterWpf.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="180" Width="540" MinWidth="440"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="FFmpeg executable path  (leave blank to use ffmpeg on system PATH)"
               Foreground="{x:Static SystemColors.GrayTextBrush}" FontSize="10" Padding="0,0,0,2"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="ffmpegBox" VerticalAlignment="Center"/>
            <Button  Grid.Column="2" Content="Browse…"    Click="Browse_Click" VerticalAlignment="Center"/>
            <Button  Grid.Column="4" Content="Test"       Click="Test_Click"   VerticalAlignment="Center"/>
        </Grid>

        <TextBlock Grid.Row="2" x:Name="statusTxt" Margin="0,6,0,0" FontSize="10"
                   Foreground="{x:Static SystemColors.GrayTextBrush}" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Save"   Width="80" IsDefault="True" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
